# ======== Server ========
# PORT — только для локальной разработки.
# На Render переменную PORT НЕ задаём (Render сам передаёт порт процессу).
# PORT=3000

# Разрешённый фронтенд (точный домен на Vercel; для теста можно *)
CORS_ORIGIN=https://<your-app>.vercel.app

# ======== 100ms ========
# Не обязателен для /api/token по Room Code, но пригодится для админ/отладки:
HMS_MANAGEMENT_TOKEN=

# Обязательные шорткоды из 100ms → Rooms → Room Codes (Enabled)
HMS_ROOM_CODE_HOST=
HMS_ROOM_CODE_GUEST=

# Больше не используется — удалить:
# HMS_API_BASE=
